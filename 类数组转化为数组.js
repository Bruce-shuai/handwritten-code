// let arr = Array.prototype.slice.call(arguments);  // 注意：arguments在这算一个对象，所以可以放在call 和 apply中作为第一参数。apply的第二参数可以是数组，也可以是类数组

// let arr = [...arguments];

// let arr = Array.from(arguments)  // 注意与 Object.create 做比较

// Array.prototype.concat.apply([],arguments)

