/**
 * 两个字符串对比, 得出结论都做了什么操作, 比如插入或者删除
 * pre = 'abcde123'
 * now = '1abc123'
 * a前面插入了1, c后面删除了de
 * 
 * 思路，找公共子串, 剩余的部分，now多的就是增加，pre多的就是减少
 */
// 此题有点懵逼
function findDiff(pre, now) {
  let sameStr = '';
}
